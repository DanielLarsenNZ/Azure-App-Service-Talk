@model IEnumerable<ImageViewer.Web.Models.Image>
<div class="container-fluid">
    <div class="row">
        @foreach (var image in Model)
        { 
        <div class="col-md-2">
            <img src="@image.SmallImageUrl"/>
        </div>
        }
    </div>
</div>